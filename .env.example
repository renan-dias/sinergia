PORT=3001
GEMINI_API_KEY=sua_chave_gemini_aqui
DEFAULT_MODEL=gemini-1.5-flash

# Banco de dados (Turso / libSQL).
# Em desenvolvimento, deixe as duas variáveis abaixo em branco: o servidor cai
# automaticamente no arquivo local sinergia.db.
# Em produção (Vercel), preencha as duas nas Environment Variables do projeto.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
